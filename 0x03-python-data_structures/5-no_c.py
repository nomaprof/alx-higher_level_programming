#!/usr/bin/python3
def no_c(my_string):
    """a function to remove character from a string"""
    modified_ans = ""
    for m in range(len(my_string)):
        if my_string[m] != 'c' and my_string[m] != 'C':
            modified_ans += modified_answer[m]
    return modified_ans
